/* eslint-disable react/function-component-definition */
const Footer = () => {
  // buscar o conteudo do carrinho no local storage
  // se vazio, não renderiza,
  // se estiver com algum produto, renderiza
  return <h1>sou o footer</h1>;
};

export default Footer;
